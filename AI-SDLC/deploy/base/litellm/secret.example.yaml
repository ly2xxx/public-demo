# COMMITTED AS A TEMPLATE ONLY. The real Secret is created out-of-band:
#   kubectl -n ai-platform create secret generic litellm-secrets \
#     --from-literal=master-key="sk-$(openssl rand -hex 24)" \
#     --from-literal=salt-key="$(openssl rand -hex 16)" \
#     --from-literal=database-url="postgresql://litellm:PASSWORD@postgres:5432/litellm"  # gate:allow-secret
# tests/test_manifests.py asserts every value here is still a placeholder.
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: ai-platform
  labels:
    app.kubernetes.io/part-of: ai-sdlc
type: Opaque
stringData:
  master-key: REPLACE_ME
  salt-key: REPLACE_ME
  database-url: REPLACE_ME
